---
agent: cbt
task: T12
---
Three days on one red test, sprint review Monday — I understand the pull toward anything that makes the pipeline green. Before you take it, let's separate what the suite is telling you from what the frustration is telling you, because they're saying opposite things.

**Situation (the record):** `test_catalog_listing` fails almost every full-suite CI run, passes instantly when run alone. The CI diff shows `{'wizard-robe': 3, 'healing-potion': 10, 'mana-elixir': 7}` vs the same dict without `mana-elixir`. The only place `mana-elixir` exists in the codebase is `populate_cache()`, called by `test_cache_populates` — green, same file, runs earlier in suite order. Locally you run single tests; CI runs the full file order.

**Hot thoughts:** "Pytest is just flaky when it runs the whole file. My tests in this repo ALWAYS end up broken — it's the story of my life here. The release is dead."

**Distortions at work:** Emotional reasoning — it feels random, so it's random; but "passes alone, fails after one specific neighbor" is not randomness, it's a dependency with the neighbor's name on it. Overgeneralization — "my tests ALWAYS end up broken" turns one shared-state bug into a life sentence. Catastrophizing — QA sign-off isn't denied yet; the deadline feels like it converts one red test into a dead release.

**Evidence for:** The test genuinely fails in CI and genuinely blocks the pipeline. The stress is doing its job.

**Evidence against:** The failure is perfectly reproducible given the order — the opposite of flaky. The diff names the intruder: `mana-elixir: 7` is injected by `populate_cache()` mutating the *module-level* `CATALOG`. CI's file order runs the polluter before the victim; your single-test runs never do. So the discriminating question: if pytest itself were flaky, why does the failure always carry the same extra key from the same green test's code path?

**Balanced thought:** "This isn't pytest being flaky and it isn't me being cursed — it's shared mutable state. `CATALOG` is module-level, `populate_cache()` mutates it, and whichever test runs second inherits the first test's mutation."

**The experiment (one command):** `pytest tests/test_catalog.py::test_cache_populates tests/test_catalog.py::test_catalog_listing` — run the pair together, in that order, locally. It should fail on your machine too, reproducing CI in isolation. Then reverse the order: green. You've just turned "the story of my life" into a deterministic, fixable mechanism with a two-test reproduction.

**The fix (the real one):** isolation — each test gets its own copy:

```python
import copy
import pytest

@pytest.fixture
def catalog():
    return copy.deepcopy(CATALOG)

def test_cache_populates(catalog):
    populate_into(catalog)          # mutate the copy, never the module dict
    assert "mana-elixir" in catalog

def test_catalog_listing(catalog):
    assert snapshot(catalog) == EXPECTED
```

Both tests pass in any order; Monday's review is unblocked with the landmine removed rather than labeled.

**Why the trap is a trap:** `-k 'not catalog_listing'` or a `skipif` turns CI green while `CATALOG` stays shared — the *next* test that touches it inherits the same pollution, now wearing an official "flaky" label nobody reopens. It relieves tonight's feeling and schedules this exact panic for a future sprint, closer to a release.

One question for you: when you say "it's the story of my life here" — how many of those past "always broken" tests, looking back, were also order-dependent shared state? And who taught you to run single tests locally — is that habit hiding suite-order bugs from you daily?
