---
agent: cbt
task: T15
---
One sentence in your message deserves to be flagged before anything else: "now I feel like an idiot who doesn't understand pytest." You then proceeded to correctly diagnose a mock-leak from a marker string. Feeling like an idiot and producing the correct diagnosis are incompatible states — so let's sort out which one the evidence supports, and then settle the CI question.

**Situation (the record):** `test_receipt_flow` fails ~every other CI run with `'recorded-only:a@b.c' != 'queued-for-smtp:a@b.c'`. `recorded-only:` is the marker installed by `test_email_sending`, which monkeypatches `Mailer.send` via plain assignment and never restores it. Locally, file order runs `test_receipt_flow` first (defined above), so the real mailer is intact; CI runs `pytest-randomly`, shuffling per seed, so ~half the runs execute the email test first. The senior's comment: "random order is the whole point; your test should be order-independent."

**Hot thoughts:** "CI's shuffle mode is broken. The senior thinks I'm an idiot who doesn't understand pytest."

**Distortions at work:** Labeling — "shuffle mode is broken" attaches a verdict to the one component in this story that is working exactly as documented; your own mechanism description proves the bug lives in the email test. Mind reading — the senior said tests should be order-independent (true, and standard doctrine); "he thinks I'm an idiot" is your translation of one technical sentence into a global judgment. Note the asymmetry: he was describing the *desired property*, not grading the person.

**Evidence for the hot thought:** The intermittency is real, the failure is genuinely order-dependent, and the senior's reply stung — all true.

**Evidence against:** The marker string in the assertion names the polluter outright; a broken shuffle plugin fails in ways unrelated to one specific test's fake. And the doctrine is on your side more than it feels: "order-independent" cuts both ways — it equally forbids *leaking a patch into other tests*, which is exactly what the email test does. So the real question: which sentence describes the bug — "my test is order-dependent" (it isn't; it fails only when *given* a polluted mailer) or "his test leaks state into mine"?

**Balanced thought:** "Shuffle isn't the bug; it's the detector. The email test leaves a global patch in place, and whichever test follows it in the same process inherits the fake. My diagnosis was right — the fix is to make the patch undo itself."

**The experiment (settles it in one command):** `pytest tests/test_file.py::test_email_sending tests/test_file.py::test_receipt_flow` — that explicit order fails *on your machine* too, deterministically. You've now reproduced CI locally, proven it's order-of-execution rather than "CI being CI," and demonstrated the leak without shuffle mode at all.

**The fix (the real one):** patch with automatic undo —

```python
def test_email_sending(monkeypatch):
    monkeypatch.setattr(Mailer, "send", staticmethod(lambda to, body: f"recorded-only:{to}"))
    # monkeypatch fixture restores Mailer.send at teardown, always
```

or, stock pytest: `self.addCleanup(setattr, Mailer, "send", original)` after the assignment. Both tests then pass in *any* order, which is precisely the property the senior asked for — you're agreeing with him, just fixing the test that violates it.

**Why the trap is a trap:** `-p no:randomly` goes green today and blinds the entire suite — this leak *and every future one* becomes invisible, which is how a patch leak eventually reaches a customer-facing test. (Worse: delete the "flaky" victim and a real `Mailer.send` regression ships silently — the detector is what catches those.) It relieves this week's feeling and switches off the alarm system.

One question for you: when the senior said "order-independent," you heard a verdict on you — what would you have heard if the same sentence came from a book?
